// Re-exported from @tinyclaw/config for backward compatibility
export { ConfigManager, createConfigTools, TinyClawConfigSchema, CONFIG_DEFAULTS } from '@tinyclaw/config';
export type { TinyClawConfigData, ConfigManagerConfig, ConfigManagerInterface } from '@tinyclaw/config';
