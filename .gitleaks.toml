# Gitleaks configuration
# https://github.com/gitleaks/gitleaks#configuration

title = "Gitleaks - TinyClaw"

[allowlist]
description = "Known false positives"
paths = [
  # Example env files contain placeholder values, not real secrets
  '''.env.example''',
]
