# TinyClaw Web UI (Svelte + Vite)

This package provides the TinyClaw web interface built with Svelte and Vite.

## Scripts
- `bun run dev` or `npm run dev`
- `bun run build` or `npm run build`
- `bun run preview` or `npm run preview`
